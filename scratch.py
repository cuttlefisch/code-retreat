#!/usr/bin/env python3

import pytest

# Initial tests for cell lifecyle

# A) If cell has fewer than two neighbors then die

# B) If cel has two or three neighbors then live

# C) if cel has greater than three neighbors then die

# D) if cell has exactly three neighbors, and is dead, then revive (set to live)
